# The ID of your Google Cloud Organization (a string of numbers).
# Example: organization_id = "123456789012"
organization_id = "1024767897869"

# A project ID that Terraform can use for billing API calls.
# This project doesn't host the resources, it just pays for the Terraform operations.
# Example: billing_project = "my-tf-billing-project"
billing_project = "terraform-473206"